apiVersion: v1
kind: Service
metadata:
  name: mongo-service

spec:
  type: {{ .Values.mongo.service.type }}

  selector:
    app: mongo

  ports:
    - port: {{ .Values.mongo.port }}
      targetPort: {{ .Values.mongo.port }}