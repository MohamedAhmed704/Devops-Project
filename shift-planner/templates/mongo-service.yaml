apiVersion: v1
kind: Service
metadata:
  name: mongo-service

spec:
  type: {{ .Values.mongo.service.type }}

  selector:
    {{ .Values.mongo.selector | toYaml | nindent 4 }}

  ports:
    - port: {{ .Values.mongo.port }}
      targetPort: {{ .Values.mongo.port }}