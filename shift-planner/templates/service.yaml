apiVersion: v1
kind: Service
metadata:
  name: {{ include "shift-planner.fullname" . }}
  labels:
    {{- include "shift-planner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shift-planner.selectorLabels" . | nindent 4 }}
